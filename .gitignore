# ── Raw dataset (download from Kaggle; too large for GitHub) ──
data/*.csv
data/*.parquet

# ── Large trained artifacts (notebook regenerates them; use Git LFS if you want them tracked) ──
models/linkedin_lstm_model.keras
models/tokenizer.pkl
*.h5

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/
.env

# ── Jupyter / Colab ──
*.ipynb~

# ── OS / editor cruft ──
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
